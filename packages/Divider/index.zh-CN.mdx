# Divider 分割线

import CodeBlock from '../../docs/components/CodeBlock/CodeBlock'
import DividerBasic from './_example/DividerBasic'
import DividerContent from './_example/DividerContent'
import DividerBorder from './_example/DividerBorder'
import DividerDirection from './_example/DividerDirection'

区隔内容的分割线。

## 基础用法

对不同段落的文本进行分割。

<CodeBlock name="DividerBasic">
  <DividerBasic />
</CodeBlock>

## 设置文案

可以在分割线上自定义文本内容。

<CodeBlock name="DividerContent">
  <DividerContent />
</CodeBlock>

## 虚线

您可以设置分隔符的样式。

<CodeBlock name="DividerBorder">
  <DividerBorder />
</CodeBlock>

## 垂直分隔线

<CodeBlock name="DividerDirection">
  <DividerDirection />
</CodeBlock>

## API

### Attributes

| 属性名           | 说明                   | 类型                                                           | 默认值     |
| ---------------- | ---------------------- | -------------------------------------------------------------- | ---------- |
| direction        | 设置分割线方向         | 'horizontal' / 'vertical'                                      | horizontal |
| border-style     | 设置分隔符样式         | 'none' / 'solid' / 'hidden' / 'dashed' / ...(css/border-style) | solid      |
| content-position | 自定义分隔线内容的位置 | 'left' / 'right' / 'center'                                    | center     |