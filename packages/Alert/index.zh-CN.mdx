# Alert 提示

import CodeBlock from '../../example/components/CodeBlock/CodeBlock'
import AlertBasic from './_example/AlertBasic'
import AlertEffect from './_example/AlertEffect'

用于页面中展示重要的提示信息。

## 基础用法

Alert 组件不属于浮层元素，不会自动消失或关闭。

Alert 组件提供四种类型，由 `type` 属性指定，默认值为 `info`。

<CodeBlock name="AlertBasic">
  <AlertBasic />
</CodeBlock>

## 主题

Alert 组件提供了两个不同的主题：`light` 和 `dark`。

通过设置 `effect` 属性来改变主题，默认为 `light`。

<CodeBlock name="AlertEffect">
  <AlertEffect />
</CodeBlock>

## Alert API

### Attributes

| 属性名 | 说明         | 类型                                     | 默认值 |
| ------ | ------------ | ---------------------------------------- | ------ |
| title  | Alert 标题。 | string                                   | —      |
| type   | Alert 类型。 | 'success' / 'warning' / 'info' / 'error' | info   |
| effect | 主题样式     | 'light' / 'dark'                         | light  |