# Segmented 分段控制器

import CodeBlock from '../../docs/components/CodeBlock/CodeBlock'
import SegmentedBasic from './_example/SegmentedBasic'
import SegmentedDisabled from './_example/SegmentedDisabled'
import SegmentedBlock from './_example/SegmentedBlock'
import SegmentedReactNode from './_example/SegmentedReactNode'
import SegmentedColor from './_example/SegmentedColor'

用于展示多个选项并允许用户选择其中单个选项。

## 基础用法

设置 keyValue 为选项值。

<CodeBlock name="SegmentedBasic">
  <SegmentedBasic />
</CodeBlock>

## 禁用状态

设置 disabled 属性来禁用一些选项。

<CodeBlock name="SegmentedDisabled">
  <SegmentedDisabled />
</CodeBlock>

## Block 分段选择器

设置block为true以适应父元素的宽度。

<CodeBlock name="SegmentedBlock">
  <SegmentedBlock />
</CodeBlock>

## 自定义内容

设置 ReactNode 位来渲染自定义内容。

<CodeBlock name="SegmentedReactNode">
  <SegmentedReactNode />
</CodeBlock>

## 自定义样式

设置 color变量 渲染自定义样式。

<CodeBlock name="SegmentedColor">
  <SegmentedColor />
</CodeBlock>