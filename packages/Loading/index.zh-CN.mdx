# Loading 加载

import CodeBlock from '../../docs/components/CodeBlock/CodeBlock'
import LoadingBasic from './_example/LoadingBasic'
import LoadingText from './_example/LoadingText'
import LoadingService from './_example/LoadingService'

加载数据时显示动效。

## 区域加载

在需要的时候展示加载动画，防止页面失去响应提高用户体验（例如表格）。

<CodeBlock name="LoadingBasic">
  <LoadingBasic />
</CodeBlock>

## 加载文案

<CodeBlock name="LoadingText">
  <LoadingText />
</CodeBlock>

## loading

通过 loading.open() 打开全屏loading

<CodeBlock name="LoadingService">
  <LoadingService />
</CodeBlock>