# Popover 气泡卡片

import CodeBlock from '../../example/components/CodeBlock/CodeBlock'
import PopoverBasic from './_example/PopoverBasic'

## 基础用法

`trigger` 属性被用来决定 `popover` 的触发方式，支持的触发方式： `hover`、`click`、`focus` 或 `contextmenu`。 如果你想手动控制它，可以设置 `:visible` 属性。

<CodeBlock name="PopoverBasic">
  <PopoverBasic />
</CodeBlock>